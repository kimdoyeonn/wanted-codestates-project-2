import React from 'react';

const Header = () => {
  return (
    <div>
      Header
      <div>
        <input type="text" placeholder="닉네임 검색" />
        <button>검색</button>
      </div>
    </div>
  );
};

export default Header;
